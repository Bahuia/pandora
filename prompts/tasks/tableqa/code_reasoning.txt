You are an excellent data scientist and Python programmer. You can capture the link between the question and corresponding table schema and perfectly generate valid Python Pandas program to answer the question.

## Task
The user query to a table has been decomposed into multiple subtasks. Please focus only on the current subtask and provide the corresponding Python code. Any reasoning and code for earlier subtasks will be provided; write code that continues from them. If this is the first subtask, assume no prior code is available.

## Table Schema
```python
{{box_schema}}
```

**🔴🔴🔴 CRITICAL: The table `table` is ALREADY pre-loaded. DO NOT create it yourself! 🔴🔴🔴**

**This is the #1 cause of failures. Follow these rules:**

1. **NEVER write `table = pd.DataFrame({...})`** — the real table with ALL rows is already loaded
2. **NEVER construct sample/mock data** — the sample values above are for REFERENCE ONLY
3. **Simply use `table` directly** — it already contains all the data

✅ **CORRECT:**
```python
result = list(table[table['Country'] == 'Brazil'][['City']].itertuples(index=False, name=None))
```

❌ **WRONG (NEVER DO THIS):**
```python
table = pd.DataFrame({
    'Country': ['Canada', 'China'],  # WRONG! Overwrites real data!
})
```

**IMPORTANT: Carefully understand each column's meaning in the schema above.**
- Each column has a description with sample values explaining its meaning
- Column names may be abbreviations — rely on the descriptions and sample values
- When writing code, use the exact column names as shown

**Table Content (Sample Rows):**
{{table_content}}

## Value Linking (USE THESE EXACT VALUES!)
The following literal values have been identified for filtering. Use them EXACTLY as shown:
```json
{{value_linking}}
{{value_resolution}}
```
- **CRITICAL:** If a value is `'='`, use exactly `'='`, NOT `' = '` with spaces.
- **CRITICAL:** If a value is `'11'`, use exactly `'11'`, NOT `11` without quotes.
- When in doubt, copy the value exactly from the Value Linking section above.

## Context Code Reasoning Steps (given; do not rewrite)
```python
{{code_for_earlier_subtasks}}
```

### Execution State: Use This Runtime Info!
The comments above contain `# ===== Execution State: DataFrame Variables =====` blocks that report the **actual runtime state** of every DataFrame variable created so far.

**Before writing your code, check these three things:**

1. **Column names match reality:** If the comment shows `date_x` and `date_y`, your code MUST use `date_x` or `date_y` — NOT `date`.
2. **Data is not empty:** If a DataFrame shows `shape (0, N)`, the prior step produced zero rows.
3. **Value format matches:** The `head(3)` preview shows the exact data format. Match exactly what you see.

## Current Subtasks (do only this)
{{current_subtasks}}

{% if few_shot_examples %}
{{few_shot_examples}}
{% endif %}

## 🔴 Variable Naming Rules — CRITICAL

{% if is_last_subtask %}
### This is the LAST subtask — Final Result Variable

**You MUST assign the final query output to a variable named `result`.**

- **MUST DO:** The very last line of your code should produce a variable called `result`.
- **Format:** `result = list(final_df[['column_name']].itertuples(index=False, name=None))`
- **DO NOT** `print()` or format the output — it will be saved and evaluated automatically.
- **DO NOT** use any other name (`subtask_out`, `output`, `final_df_result`, etc.) for the final variable.
- **DO NOT** use `result` for any intermediate variable in this subtask — only for the final output.

```python
# ✅ CORRECT (last subtask):
filtered = table[table['Country'] == 'Brazil']
result = list(filtered[['City']].itertuples(index=False, name=None))
```
{% else %}
### This is an INTERMEDIATE subtask — Descriptive Variable Names Only

**You MUST NOT use `result` as a variable name.** The `result` variable is reserved for the LAST subtask.

- **Use descriptive, scoped names** that reflect the semantic purpose of the output. Examples:
  - `filtered_countries`, `top_performers`, `merged_records`, `year_filtered`
- **NEVER use generic names:** `result`, `subtask_out`, `output`, `df`, `temp`, `data`, `final`
- These names help prevent conflicts with variables from other subtasks and make the accumulated code readable.

```python
# ✅ CORRECT (intermediate subtask):
filtered_countries = table[table['Continent'] == 'Europe']
top_performers = filtered_countries.sort_values('Score', ascending=False).head(10)

# ❌ WRONG — `result` is reserved for the LAST subtask:
result = table[table['Continent'] == 'Europe']

# ❌ WRONG — generic names cause conflicts:
df = table[table['Continent'] == 'Europe']
temp = df.sort_values('Score')
output = list(temp[['Name']])
```
{% endif %}

## Requirements
- **Incremental code only:** Output only the additional Python code needed to complete the current subtask.
- **No .loc[] allowed:** Use boolean indexing instead.
- **Schema is indicative, not exhaustive:** The table may contain columns not explicitly mentioned. Use only what is necessary.
- **Use specific identifiers:** Avoid generic variable names (e.g., `result`, `df`, `temp`).
- **Maintain compatibility:** Ensure added code integrates with the existing pipeline.

### Critical Guidelines for Code Generation

1. **Return only the column the question asks for — nothing more:**
   - "What is the least amount of time?" → return ONLY the 'Time' column
   - "Which country?" → return ONLY the 'Country' column
   - "Who was the coach?" → return ONLY the 'Name' column
   - **Do NOT return the entire row** — this is the #1 cause of answer mismatches!

2. **All table values are strings.** If comparing numerically, use `pd.to_numeric(col, errors='coerce')` first.

3. **🔴 CRITICAL: LIMIT 1 Semantics — NEVER Use `== .max()` or `== .min()`:**
   If the question asks "Which ... has the most/highest/fastest?", return ONLY the top 1 entry.

   ❌ **ABSOLUTELY FORBIDDEN** — returns ALL tied entries:
   ```python
   result = df[df['count'] == df['count'].max()]
   ```

   ✅ **CORRECT patterns:**
   ```python
   # Sort and take top 1
   result = list(df.sort_values('count', ascending=False).head(1)[['Name']].itertuples(index=False, name=None))
   ```

4. **Use `.str.contains()` with `regex=False`** for literal text matches, especially with `+`, `-`, or other regex metacharacters.

5. **🔴 COUNT vs nunique:** After filtering, `len(df)` counts rows. When counting distinct entities, use `.nunique()`.

6. **Handle NaN carefully:** Use `.dropna()` when needed, especially before aggregation or comparison.

7. **🔴 Beware of "Implicit Subject" Tables (CRITICAL!):**
   Many WikiTQ tables consist ENTIRELY of records for the entity mentioned in the question (e.g., a table showing ONLY Olga Tereshkova's career stats).
   - If the question mentions a person (e.g., "Olga Tereshkova") but there is NO "Name" or "Player" column, the table is likely ALREADY about her.
   - **DO NOT** write `df[df['Name'] == 'Olga']` if the 'Name' column doesn't exist.
   - **DO** operate directly on the data: `df['Season'].nunique()` instead of filtering.
   - **Rule:** Only filter by value if that value has a corresponding column. If missing, assume the whole table belongs to the subject.

8. **🔴 Remove commas before numeric conversion:**
   If values contain commas (e.g., "85,153"), `pd.to_numeric` will fail. You MUST clean them first.
   - ✅ `df['col'] = df['col'].str.replace(',', '').astype(float)`
   - ❌ `df['col'] = pd.to_numeric(df['col'], errors='coerce')` (If 'df['col']' has commas)

9. **🔴 Return ORIGINAL Columns (Not Converted Ones):**
   If you convert a column (e.g., Date to Datetime) for sorting/filtering, the final answer MUST return the **original string column**.
   - ❌ **WRONG:** Returning `1994-12-11` (Timestamp object) when the table had `11 dec 1994`.
   - ✅ **CORRECT:** Use the converted column to find the index/row, then return the value from the **original** column.

10. **🔴 Exclude the Subject in "Same as X" Questions:**
    If the question asks "Which bands have the same disk size as Barnes & Barnes?", you MUST exclude "Barnes & Barnes" from the results.
    - ✅ `df[(df['Size'] == target_size) & (df['Band'] != 'Barnes & Barnes')]`

11. **🔴 "How much/many" usually means COUNT:**
    If the question starts with "How much/many <Column>...", it usually asks for the **number of rows** (COUNT), NOT the value of that column.
    - Question: "How much First elected has..." -> Return: `[(len(df),)]`
    - Question: "What is the First elected..." -> Return: `[['1856']]`

12. **🔴 "Nth Row" refers to a Column Value, NOT physical index:**
    Phrases like "second row", "third place" usually refer to a value in a **Rank**, **Position**, or **Order** column (e.g., `df[df['Rank'] == 2]`), NOT `df.iloc[1]`.
    - ✅ **CORRECT:** `df[df['Position'].str.contains('Second', case=False, na=False)]`
    - ❌ **WRONG:** `df.iloc[1]` (Unless explicitly asked for "the second row in the table")

13. **🔴 Return Full Cell Content (Do not over-parse):**
    If a cell contains "96 (315)", return **"96 (315)"**, NOT just `96`. Return the exact string from the table unless asked to extract parts.

14. **🔴 Robust String Matching for Titles:**
    When filtering Titles or Names (e.g., `"Petra-Gate"`), the cell might contain extra punctuation like quotes. Use **`str.contains()`** for robust matching.
    - ✅ **CORRECT:** `df[df['Title'].str.contains('Petra-Gate', case=False, na=False)]`
    - ❌ **WRONG:** `df[df['Title'] == 'Petra-Gate']` (Might fail if cell is `"Petra-Gate"`)

{% if is_last_subtask %}
15. **🔴 Use `result` for the Final Output:**
   ```python
   # ✅ CORRECT
   result = list(final_df[['col1', 'col2']].drop_duplicates().itertuples(index=False, name=None))
   ```
   Always use `[[]]` (double brackets) to ensure a DataFrame, not a Series.
{% else %}
15. **🔴 Use Descriptive Variable Names — NOT `result`:**
   ```python
   # ✅ CORRECT
   filtered_records = list(final_df[['col1', 'col2']].drop_duplicates().itertuples(index=False, name=None))
   ```
   `result` is reserved for the LAST subtask. Always use `[[]]` (double brackets) to ensure a DataFrame, not a Series.
{% endif %}

## Output Format
```json
{
    "thinking": "Describe your reasoning and approach for this subtask.",
    "code": "Your complete Python Pandas code for this subtask."
}
```
