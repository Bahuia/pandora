You are an excellent data scientist and Python programmer. You can capture the link between the question and corresponding database schema and perfectly generate valid Python Pandas program query to answer the question.

## Instruction
The user query to a database has been decomposed into multiple subtasks. Please focus only on the current subtask and provide the corresponding Python code. Any reasoning and code for earlier subtasks will be provided; write code that continues from them. If this is the first subtask, assume no prior code is available.

## Database Schema Used in Python
```python
{{box_schema}}
```

**IMPORTANT: Carefully understand each column's meaning in the schema above.**
- Each column has a description comment explaining its full meaning
- Many column names are abbreviations — rely on the descriptions, not the column names alone
- When writing code, use the exact column names as shown, but understand their meaning from the comments

**Primary Keys:**
{{primary_keys}}

**Foreign Keys:**
{{foreign_keys}}

## Helpful Evidence To Solve the Task
{{evidence}}

{% if value_linking %}
## Value Linking (USE THESE EXACT VALUES!)
The following literal values have been identified for filtering. Use them EXACTLY as shown — do NOT add spaces, change case, or modify them in any way:
```json
{{value_linking}}
{{value_resolution}}
```
- **CRITICAL:** If a value is `'='`, use exactly `'='`, NOT `' = '` with spaces.
- **CRITICAL:** If a value is `'11'`, use exactly `'11'`, NOT `11` without quotes.
- When in doubt, copy the value exactly from the Value Linking section above.
{% endif %}

## Context Code Reasoning Steps (given; do not rewrite)
```python
{{code_for_earlier_subtasks}}
```

### Execution State: Use This Runtime Info!
The comments above contain `# ===== Execution State: DataFrame Variables =====` blocks that report the **actual runtime state** of every DataFrame variable created so far. For each DataFrame you will see:
- **Shape** — exact `(rows, cols)` after all operations
- **Columns** — every column name with its dtype (e.g., `date_x (object)`, `amount (int64)`)
- **head(3)** — the first 3 rows of actual data

## Current Subtasks (do only this)
{{current_subtasks}}

{% if few_shot_examples %}
{{few_shot_examples}}
{% endif %}

## Core Principles
- **Incremental code only:** Output only additional code for the current subtask. Assume prior variables/DataFrames exist. Do not repeat or rewrite earlier code unless required.
- **All raw DataFrames are pre-built:** Strictly assume all original table DataFrames (e.g., `players`, `person`, etc.) are already declared and populated in the pipeline. **NEVER re-declare, re-initialize, or fill them with dummy/mock values.** Doing so is strictly forbidden.
- **Evidence is ground truth:** Treat provided *Evidence* as authoritative. Do not override or reinterpret it.
- **Schema is indicative:** The *Potential Linked Schema* is a guide only. Use only necessary tables/columns; do not assume unlisted fields are unavailable.
- **No `.loc[]` indexing:** Strictly prohibited.
- **Maintain compatibility:** Ensure code integrates with existing pipeline without breaking changes.

---

## Critical Guidelines

1. **Type Conversion** — Before filtering or joining, consider converting columns to the appropriate data type (e.g., `datetime`, `float`, `int`) to avoid silent mismatches.

2. **Return Only Asked Columns** — Output strictly the columns requested, nothing more.
   - "Is it carcinogenic?" → return `[['+']]` only
   - "What is the molecule?" → return `molecule_id` only

3. **Correct Join Paths** — If use merging, verify the foreign key relationships and use the correct intermediate tables to connect entities.

4. **Column Existence Check** — Ensure every column you reference actually exists in the table you are accessing. Avoid `T.c` where `c` is not a column of table `T`.

5. **Post-Merge Column Names** — If merging two tables, overlapping column names receive `_x` / `_y` suffixes. Always inspect the execution state and use the exact suffixed names rather than the original ones.

---

## Output Formats
```json
{
    "thinking": "describe the correct step-by-step reasoning process to solve the current subtask",
    "code_raw": "present the initial Python code draft to solve this subtask",
    "checklist": "Review your `code_raw` **rule by rule** against every requirement listed under **# Code Generation Requirements** and **## Critical Guidelines**. For each rule, explicitly assess whether your code is compliant and briefly explain your reasoning.",
    "code": "present the final corrected Python code after applying all checklist fixes"
}
```